# territory-os
Local, single-file territory triage dashboard for a CoLab AE — transparent account scoring, a field-intel overlay, per-account AE playbooks, and a weekly BDR cadence. Runs in the browser, no backend or keys. Private repo (contains confidential account data).
